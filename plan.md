# طرح پیاده‌سازی صفحه معرفی محصول (Introduction) با تم کلود (Claude Aesthetic)

این سند شامل نقشه راه، ساختار فایل‌ها و جزئیات فنی پیاده‌سازی لندینگ پیج معرفی محصول VOD/Mediafy با زبان فارسی، تایپوگرافی زیبا، تم مینیمال و آیکونیک کمپانی Anthropic (Claude) و بخش جستجو و نمایش آزمایشی (Mini-Browse) محصولات است.

---

## ۱. ویژگی‌های طراحی و تم کلود (Claude Aesthetic)
* **رنگ‌بندی:** استفاده از رنگ‌های گرم خنثی مانند پس‌زمینه استخوانی/شنی ملایم (`#FBF9F6`) در تم روشن، متن‌های ذغالی تیره (`#191919`)، حاشیه‌های باریک و دقیق شنی (`#E6E1DA`) و رنگ سازمانی زرشکی/تراکوتا ملایم (`#D97756` یا `#C85A3F`) به عنوان رنگ آکسان.
* **تایپوگرافی:** استفاده از فونت‌های فارسی وب استاندارد و خوانا (مانند Vazirmatn یا Estedad) با وزن‌های هماهنگ و فاصله‌گذاری‌های متنی دست‌ودلباز.
* **چیدمان (Layout):** کارت‌های مینیمال با لبه‌های ملایم، سایه‌های بسیار محو و استفاده خلاقانه از فضاهای خالی (Whitespace).

---

## ۲. بخش‌های مختلف لندینگ پیج (Landing Page Sections)

### الف. هدر (Header)
* لوگوی مینیمال متنی و گرافیکی محصول.
* منوی دسترسی سریع به ویژگی‌ها، مستندات، و بخش دمو.
* دکمه خرید/تماس با دکمه استایل کلود (کپسولی با حاشیه ظریف).

### ب. بخش قهرمان (Hero Section)
* عنوان بزرگ و جذابِ فارسی توصیف‌کننده محصول (مثلا: "مدیریت و انتشار ویدیو، فراتر از استانداردهای معمول").
* متن کوتاه توضیح ویژگی‌های کلیدی.
* دکمه‌های فراخوان عمل (CTA): "پیش‌نمایش زنده" و "مشاهده مستندات".
* تصویر یا موکاپ انیمیشنی باکیفیت از محیط داشبورد مدیریت و سایت.

### ج. بخش امکانات کلیدی (Bento Grid Features)
کارت‌های شبکه‌ای مینیمال برای معرفی قابلیت‌های کلیدی:
* **آرشیو خودکار با TMDB:** چگونگی دریافت سریع اطلاعات فیلم و سریال.
* **پخش‌کننده پیشرفته فیلم:** پلیر پیشرفته و اختصاصی VOD با قابلیت سوییچ کیفیت و زیرنویس.
* **سیستم فروش و اشتراک ویژه:** کیف پول، درگاه بانکی و پلن‌های VIP.
* **داشبورد مدیریت یکپارچه:** ابزارهای کامل ادمین برای نظارت بر کل سیستم.

### د. بخش پیش‌نمایش تعاملی (Interactive Mini-Browse)
یک مینی‌اپلیکیشن شبیه‌سازی‌شده درون لندینگ پیج که بدون نیاز به ورود به سایت اصلی، به مشتری اجازه تجربه بخش کاربری را می‌دهد:
* **فیلترهای تب‌بندی:** دکمه‌های جابجایی بین "فیلم‌ها"، "سریال‌ها"، "محبوب‌ترین‌ها".
* **باکس جستجوی زنده:** فیلتر آنی فیلم‌های شبیه‌سازی شده بر اساس تایپ کاربر.
* **کارت‌های فیلم شیک:** نمایش تصویر پوستر، امتیاز IMDB، ژانرها و سال تولید.
* **نمایش جزئیات پاپ‌آپ (Modal):** با کلیک روی هر کارت، جزئیات فیلم (خلاصه داستان، بازیگران) به همراه پیش‌نمایش ویدیویی پخش‌کننده لود می‌شود.

### ه. بخش مشخصات فنی و معماری (Technical Specs)
* توضیح پشته فناوری (Next.js, Tailwind, MySQL).
* سئو عالی، سرعت بالا و قابلیت نصب آسان روی Vercel/PaaS.

### و. فوتر (Footer)
* لینک‌های شبکه‌های اجتماعی، کپی‌رایت و اطلاعات تماس.

---

## ۳. معماری فایل‌ها و پوشه‌ها

پروژه به صورت یک اپلیکیشن کاملاً مستقل Next.js در پوشه `Introduction` پیکربندی خواهد شد:

```text
Introduction/
├── public/                 # دارایی‌های استاتیک، تصاویر و آیکون‌ها
├── src/
│   ├── app/
│   │   ├── layout.tsx      # ساختار پایه، فونت‌ها و هدایت‌کننده‌های متا
│   │   ├── page.tsx        # صفحه اصلی لندینگ پیج
│   │   └── globals.css     # استایل‌های سراسری و متغیرهای رنگی کلود
│   ├── components/
│   │   ├── Header.tsx      # منوی بالای صفحه
│   │   ├── Hero.tsx        # بخش اصلی معرفی محصول
│   │   ├── Features.tsx    # کارت‌های معرفی قابلیت‌ها
│   │   ├── MiniBrowse.tsx  # بخش شبیه‌ساز سرچ و نمایش تعاملی فیلم‌ها
│   │   └── Footer.tsx      # پانوشت
│   └── data/
│       └── mockData.ts     # دیتای فیک فیلم‌ها و سریال‌ها برای بخش مینی‌براوز
├── package.json            # وابستگی‌های پروژه (Next.js, Tailwind, Lucide React)
├── tailwind.config.ts      # تنظیمات رنگی و تم کلود برای کلاس‌های کمکی
└── tsconfig.json           # پیکربندی تایپ‌اسکریپت
```

---

## ۴. مراحل پیاده‌سازی در زمان شروع کار

1. **مرحله اول:** ساختاردهی اولیه فایل‌ها و پیکربندی Tailwind با پالت رنگی شنی/ذغالی کلود.
2. **مرحله دوم:** ایجاد دیتای ماک فیلم‌ها (`mockData.ts`) شامل نام، ژانر، سال، پوستر و خلاصه داستان.
3. **مرحله سوم:** پیاده‌سازی کدهای فرانت‌اند بخش هدر، هیرو و Bento Grid ویژگی‌ها.
4. **مرحله چهارم:** ساخت کامپوننت تعاملی `MiniBrowse` به همراه قابلیت فیلتر آنی، سرچ زنده و مودال جزئیات فیلم.
5. **مرحله پنجم:** بهینه‌سازی ریسپانسیو بودن لایوت برای نمایش عالی روی موبایل و دسکتاپ.
6. **مرحله ششم:** تست نهایی صحت بیلد جهت دیپلوی سریع و یک‌کلیکه روی Vercel.
